* Under the admin page, make an option to create shifts for every date and not just the one that is selected.

* Format the layout of the admin page (especially the calendar) to look nicer

* When Admin deletes an event, also remove the event calendar, etc elements from the display

* After adding a shift to all dates, update the calendar for every date, and not just the current one. At the moment, a page reload is required.

* Add preference to select singer for shifts (use an asterisk)